version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Installing dependencies...
  build:
    commands:
      - echo Build phase
  post_build:
    commands:
      - echo Preparing artifacts for CodeDeploy
      - zip -r build.zip * .[^.]*

artifacts:
  files:
    - build.zip
